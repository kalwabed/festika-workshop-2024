export interface Event {
  id: string;
  title: string;
  location: string;
  description: string;
  from: string;
  to: string;
  date: string;
  tags: string
  createdAt: string;
  cover: string;
}
